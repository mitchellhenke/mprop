defmodule Properties.Repo.Migrations.Shapefiles do
  use Ecto.Migration

  def change do
    # create table(:shapefiles) do
    #   add :gid, :serial
    #   add :taxkey, :string, size: 10
    #   add :map_id, :bigint
    #   add :parcel_typ, :integer
    #   add :gis_dateti, :date
    #   add :shape_area, :decimal
    #   add :shape_len, :decimal
    #   add :geom_point, :geometry
    # end

    # alter table(:shapefiles) do
    #   remove(:id)
    #   modify(:gid, :bigint, primary_key: true)
    # end
  end
end
