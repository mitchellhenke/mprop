<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <%= live_title_tag assigns[:page_title] || "Milwaukee" %>
    <link rel="stylesheet" href="<%= Routes.static_path(@socket, "/assets/app.css") %>">
    <%= csrf_meta_tag() %>
  </head>
  <body>
    <header>
      <section class="container">
        <nav role="navigation">
          <ul>
            <li><%= link("Home", to: Routes.page_path(@socket, :index)) %></li>
          </ul>
        </nav>
      </section>
    </header>
    <div class="container">
      <main role="main">
        <p class="alert alert-info" role="alert"
          phx-click="lv:clear-flash"
          phx-value-key="info"><%= live_flash(@flash, :info) %></p>

        <p class="alert alert-danger" role="alert"
          phx-click="lv:clear-flash"
          phx-value-key="error"><%= live_flash(@flash, :error) %></p>
        <%= @inner_content %>
      </main>
      <div class="footer">
        <%= link "Source Code", to: "https://github.com/mitchellhenke/mprop" %>
      </div>
    </div>
    <script type="text/javascript" src="<%= Routes.static_path(@socket, "/assets/live.js") %>"></script>
  </body>
</html>
